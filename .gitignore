# Rust
/target
**/*.rs.bk
Cargo.lock

# C/C++
*.o
*.a
*.so
*.dylib
*.dll

# Python
# 注意:.gitignore 不支持行尾注释 —— 模式后面写 # 会被当成模式的一部分
# .pydeps 是 build.py --deps 解包的 pybind11/numpy(不装到系统)
/.pydeps/
/.pydeps-wheels/
# 扩展模块产物
python/lmflow/*.so
python/lmflow/*.pyd
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.pytest_cache/

# 编辑器 / 工具
.vscode/
.idea/
compile_commands.json
.cache/
